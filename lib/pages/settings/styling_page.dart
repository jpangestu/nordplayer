import 'package:flutter/material.dart';

class StylingSettingPage extends StatelessWidget {
  const StylingSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Styling')),
    );
  }
}
